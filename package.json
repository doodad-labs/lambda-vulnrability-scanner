{
    "name": "lambda-vulnerability-scanner",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "AWS Lambda that performs a GET request for a provided URL and logs with associated email.",
    "engines": {
        "node": ">=22 <23"
    },
    "scripts": {
        "build": "tsc",
        "clean": "rimraf dist",
        "package": "npm run build && sam build",
        "deploy": "npm run package && sam deploy --no-confirm-changeset --stack-name LambdaVulnerabilityScannerStack --capabilities CAPABILITY_IAM"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.136",
        "@types/node": "^20.14.10",
        "rimraf": "^5.0.5",
        "typescript": "^5.5.4"
    }
}