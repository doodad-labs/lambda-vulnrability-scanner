import { IndividualScanResult } from '../types/scans';

export default async function(url: URL, body: string, headers: Headers): Promise<IndividualScanResult> {
    let found: boolean = false;
    const messages: string[] = []

    // Return results
    return {
        found: found,
        messages: messages.length > 0 ? messages : ['No vulnerabilities found']
    }
}